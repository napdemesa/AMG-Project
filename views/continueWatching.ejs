<% include ./partials/header %>

<div class="container">
    
    <div class="jumbotron">
        <h1 class="display-4">Continue Watching</h1>
        <hr class="my-4">
        <p class="lead">Catch up on the shows you put on hold!</p>
    </div>
    
    <div class="row container middleInfo">
        <% dashboard.Data.TitleRows[0].Titles.forEach(function(item){ %>
            <% include ./partials/middleware/index %>
        <% }); %>
    </div>
    
</div>

<% include ./partials/footer %>